import React from "react";

export default function Hero() {
    return (
        <div className="cl-hero">
            <h1>Know your <em>exact</em> gap.<br />Close it fast.</h1>
            <p>Drop your resume + job description. Get an instant match score and a week-by-week roadmap to land the role.</p>
        </div>
    );
}
